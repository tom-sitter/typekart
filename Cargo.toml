[package]
name = "typekart"
version = "0.1.0"
edition = "2024"
description = "A terminal typing racer with kart-style item effects"
readme = "README.md"
keywords = ["typing", "game", "terminal", "tui"]
categories = ["command-line-utilities", "games"]
include = [
    "Cargo.toml",
    "Cargo.lock",
    "README.md",
    "src/**",
    "words_alpha.txt",
]

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
crossterm = "0.27"
rand = "0.8"
ratatui = "0.26"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
pretty_assertions = "1"
