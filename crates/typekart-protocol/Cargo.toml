[package]
name = "typekart-protocol"
version = "0.1.6"
edition = "2024"
description = "Shared TypeKart JSON protocol types"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
