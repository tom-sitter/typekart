# TypeKart v0.1.7

Released: 2026-06-05

## Highlights

- Terminal typing races with kart-style item effects.
- Single-player races with optional AI racers.
- Local, LAN, and relay-backed online multiplayer.
- Unicode item indicators by default, with an ASCII fallback.
- Moddable word sets and configurable item packs.
- macOS and Windows release archives.

## Install

Download the archive for your platform from the GitHub release:

- macOS Apple Silicon: `typekart-aarch64-apple-darwin.tar.gz`
- macOS Intel: `typekart-x86_64-apple-darwin.tar.gz`
- Windows x64: `typekart-x86_64-pc-windows-msvc.zip`

## Changes

- Better web ui (99b8e7a)
- better lobby controls (c9991fe)
- better connection statuses (1605409)
- more web lobby commands (196db27)
- add room hosting (37d6050)
- host race has countdown (9d2d54e)
- human players can race in browser hosted games (9431983)
- AIs work in web hosted games (438792a)
- big refactors to shared game lib between terminal/web (359540e)
- more game engine refactors (a87e737)
- split main into out into dedicated modules (5685d72)
- final thinning of main.rs into modules (3fc3123)
- more game engine refactors/de-dup (2e24470)
- extra bonus items flow to dedicated module (c54941f)
- more refactor into shared state (5b858d2)
- move race flow and shapshotting into game engine (b37654f)
- race snapshots into game engine (442d3a1)
- extract host lifecycle events from server.rs (747a3e0)
- extra bonus network flow into module (66d5dcc)
- extract item behaviour (2706d0d)
- extract AI typing behaviour (652363b)
- Extra host lobby networking form server (930013e)
- fix flakey test (f77a70e)
- extract snapshot networking (d758ebd)
- fix cyclone targetting behaviour (2d5b654)
- extract race lifecycle networking (ea515fc)
- extract txp handshake validation (23680b7)
- extract network broadcasting (095621f)
- extract host lobby networking (d55c331)
- more server breakdwn (89395ac)
- extract host state and accept loop into separate modules (9c41fcc)
- move session logic in shared game engine (c9be9b3)
- refactor return-to-lobby logic to game engine (7a22246)
- add shared race tick outcome handling to game engine (c73e143)
- add active-race lifecycle advancement to host session (731b319)
- add start-race outcome handling to GE (c2f6fc4)
- add countdown-cancel outcome handling to host session (0274276)
- add shared host runtime reset outcomes for waiting and active races (4536df9)
- add shared race-finished event handling to host lifecycle outcomes (79d3a76)
- unify race phase handling with NetworkRacePhase across UI components (834eea9)
- move last local items over to game engine (781900f)
- refactor: unify mushroom handling for AI racers and shared state (562d26d)
- move remaining SP item effects to shared GE (1ca46c0)
- refactor: unify local and network bonus claim handling with shared logic (550d4c5)
- higher level GE function, migrate to use them (58424d5)
-  unify item pickup handling across host sessions (19c1a8f)
- unify host session aftermath handling and race result finalization (0e66424)
- refactor: introduce shared host-visible events for item interactions, item, claims and race outcomes (037b346)
- unify aftermath handling across host sessions with shared adapter actions (0ff2190)
- implement shared AI tick handling across host sessions (812d97f)
- replace Squid Ink with Fog in gallery scenarios and related logic (9973028)
- refactor browser host race state authority (5105e2d)
- refactor local ai driver state (99921f0)
- share host event feed capping (8da7de8)
- share bounded tick elapsed helper (43eda8e)
- mark shared logic refactor plan complete (2541409)
- extract tests into sibling files (0e1b147)

## Known Beta Limitations

- Online play requires a relay that players can reach.
- The relay is in-memory; restarting it closes active rooms.
- WinGet manifests should be updated from this release's checksum file before submitting to microsoft/winget-pkgs.
